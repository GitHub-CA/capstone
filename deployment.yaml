apiVersion: v1
kind: Service
metadata:
  name: capstone-svc
  labels:
    app: capstone-app
spec:
  selector:
    app: capstone-app
  ports:
    - port: 8000
      targetPort: 80
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstone
  labels:
    app: capstone-app
spec:
  replicas: 2 
  selector:
    matchLabels:
      app: capstone-app
  template:
    metadata:
      labels:
        app: capstone-app
    spec:
      containers:
      - name: capstone
        image: mbeimcik/capstone
        imagePullPolicy: Always
        ports:
        - containerPort: 80
